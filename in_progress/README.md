# In Progress

Temporary compatibility tools live here while their reusable logic is migrated into `eli_lab/` and their GUIs are replaced by native application modules.

These files are intentionally kept out of the repository root so the root stays clean and the migration boundary is obvious.
