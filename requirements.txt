# Runtime dependencies
Pillow>=11.3,<13
python-magic>=0.4.27,<0.5
tkcalendar>=1.6.1,<2

# Optional external tools
# pngquant must be installed separately and available on PATH for texture optimization.
